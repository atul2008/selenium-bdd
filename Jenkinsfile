pipeline {
    agent 'any'

    stages {
        stage('Hello') {
            steps {
                echo "Hello I am multibranch pipeline"
                echo " value is: ${env.Var}"
                echo " Val here is: ${Var}"

            }
       }
    }
}